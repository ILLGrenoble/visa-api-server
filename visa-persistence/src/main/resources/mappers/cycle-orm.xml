<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xmlns="http://xmlns.jcp.org/xml/ns/persistence/orm"
                 xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence/orm http://xmlns.jcp.org/xml/ns/persistence/orm_2_1.xsd"
                 version="2.1">

    <named-query name="cycle.getById">
        <query>SELECT c FROM Cycle c WHERE c.id = :id</query>
    </named-query>

    <named-query name="cycle.getCurrent">
        <query>
            <![CDATA[
                SELECT c FROM Cycle c
                WHERE c.startDate <= CURRENT_DATE
                AND c.endDate >= CURRENT_DATE
            ]]>
        </query>
    </named-query>

    <named-query name="cycle.getAll">
        <query>SELECT c FROM Cycle c</query>
    </named-query>

    <named-query name="cycle.getAllForUser">
        <query>
            SELECT c FROM Cycle c
            WHERE c IN (
            SELECT e.cycle FROM Experiment e
            JOIN e.users u
            WHERE u = :user)
            ORDER BY c.startDate DESC
        </query>
    </named-query>

    <named-query name="cycle.getForDate">
        <query>
            <![CDATA[
                SELECT c FROM Cycle c
                WHERE c.startDate <= :date
                AND c.endDate >= :date
            ]]>
        </query>
    </named-query>

    <entity class="eu.ill.visa.core.domain.Cycle">
        <table name="cycle"/>
        <attributes>
            <id name="id">
                <column name="id" nullable="false"/>
            </id>
            <basic name="name">
                <column name="name" nullable="false" length="100"/>
            </basic>
            <basic name="startDate">
                <column name="start_date" nullable="false"/>
            </basic>
            <basic name="endDate">
                <column name="end_date" nullable="false"/>
            </basic>
        </attributes>
    </entity>

</entity-mappings>
